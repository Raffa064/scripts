if [ "$1" == "show" ]; then
 print 36 '[ Displaying /notes.txt content ]'
 if [ -e "/notes.txt" ]; then
  echo $(cat /notes.txt)
 else
  print 34 "[ notes.txt not found ]"
 fi
else
  nano /notes.txt
fi
